// import axios from 'axios';

// const api = axios.create({
//   baseURL: 'http://localhost:3000/api',
// });

// export const guardService = {
//   getGuards: () => api.get('/guards'),
//   updateLocation: (guardId: string, location: { lat: number; lng: number }) =>
//     api.post(`/guards/${guardId}/location`, location),
// };

// export const shiftService = {
//   getCurrentShifts: () => api.get('/shifts/current'),
//   assignShift: (guardId: string, shiftData: Partial<Shift>) =>
//     api.post(`/shifts/assign/${guardId}`, shiftData),
// };
